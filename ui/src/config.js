/**
 * Stock UI configuration
 * 
 * As I learn more this will be better to use .env or webpack to configure dev vs prod
 */
const config = {
    /**
     * Define the endpoint for stock api
     */
    apiUrl: 'https://localhost:5001'
};

export default config;